/// <reference types="vite/client" />

// Идентификатор сборки, подставляемый на этапе сборки (см. vite.config.ts).
// По нему приложение понимает, что на сервере лежит уже другая версия.
declare const __BUILD_ID__: string;
