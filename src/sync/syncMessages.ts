// ============================================================
// НАЗВАНИЯ ОПЕРАЦИЙ СИНКА ДЛЯ ЖУРНАЛА СБОЕВ
//
// Тот же приём, что и STORAGE_ACTIONS в lib/storageMessages.ts: до этого
// источник 'sync' в журнале сбоев (lib/errorLog.ts) был объявлен, но
// logError('sync', ...) не вызывался нигде — все сбои синка оседали только
// в lastError на экране настроек и пропадали при следующей перезагрузке.
// Названия действий заведены в одном месте, чтобы при появлении нового
// места сбоя не плодились рядом слегка разные формулировки одного и того
// же действия.
// ============================================================

export const SYNC_ACTIONS = {
  runSync: 'синхронизация с облаком',
  checkPairing: 'проверка привязки устройства',
  pairDevice: 'привязка устройства',
} as const;

export type SyncAction = (typeof SYNC_ACTIONS)[keyof typeof SYNC_ACTIONS];
